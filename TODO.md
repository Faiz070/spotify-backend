# TODO: Fix Auth Registration Error

- [x] 1. Fix export in src/controllers/auth.controller.js (export {register} instead of {registerUser})
- [x] 1.5. Fix src/routes/auth.routes.js to import/use authController.register instead of registerUser
- [x] 2. Test the server by running it and hitting /register endpoint (server starts successfully on port 3000)
- [x] 3. Verify no other errors (startup clean, route handler fixed)
